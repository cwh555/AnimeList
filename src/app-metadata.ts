import { version as packageVersion } from "../package.json";

export const PLUGIN_VERSION = packageVersion;
export const USER_AGENT = `AnimeList-Obsidian/${PLUGIN_VERSION} (local personal media library)`;
