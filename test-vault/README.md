# AnimeList test vault

This vault contains three example anime entries and local cover images. It is intended only for plugin development and is not included in normal plugin installation.

Run the following commands from the repository root:

```bash
npm ci
npm run test-vault:link
npm run dev
```

Then open this `test-vault` folder in Obsidian, allow community plugins, and enable AnimeList.
