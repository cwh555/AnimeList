name: CI

on:
  push:
    branches: [main, preview]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Capture picker lint
        id: lint
        shell: bash
        run: |
          set +e
          npm run lint > picker-lint.log 2>&1
          status=$?
          cat picker-lint.log
          exit "$status"
      - name: Upload picker lint
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: picker-lint-log
          path: picker-lint.log
          retention-days: 1
