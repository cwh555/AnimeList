name: Rebuild progress feature branch

on:
  push:
    branches:
      - feature/unified-progress-display
    paths:
      - .github/workflows/rebuild-progress-branch.yml

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 